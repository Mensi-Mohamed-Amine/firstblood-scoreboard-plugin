# Build the image
docker build -t firstblood-scoreboard .

# Run the container
docker run -d -p 5000:5000 --name firstblood --restart unless-stopped firstblood-scoreboard


docker compose up -d --build
